CREATE SCHEMA IF NOT EXISTS covid;
CREATE TABLE IF NOT EXISTS covid.Alabama (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Alaska (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Arizona (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Arkansas (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.California (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Colorado (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Connecticut (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Delaware (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Florida (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Georgia (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Hawaii (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Idaho (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Illinois (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Indiana (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Iowa (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Kansas (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Kentucky (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Louisiana (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Maine (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Maryland (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Massachusetts (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Michigan (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Minnesota (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Mississippi (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Missouri (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Montana (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Nebraska (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Nevada (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.New_Hampshire (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.New_Jersey (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.New_Mexico (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.New_York (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.North_Carolina (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.North_Dakota (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Ohio (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Oklahoma (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Oregon (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Pennsylvania (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Rhode_Island (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.South_Carolina (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.South_Dakota (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Tennessee (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Texas (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Utah (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Vermont (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Virginia (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Washington (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.West_Virginia (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Wisconsin (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Wyoming (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.American_Samoa (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.District_of_Columbia (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Guam (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Northern_Mariana_Islands (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Palau (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Puerto_Rico (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS covid.Virgin_Islands (
    id SERIAL PRIMARY KEY,
    total_cases BIGINT,
    new_cases BIGINT,
    total_deaths BIGINT,
    new_deaths BIGINT,
    date date NOT NULL UNIQUE
);